# NetCDF 至 Cesium 时序贴图生成规范

## 1. 概述

### 1.1 背景与目标
本规范定义将 NetCDF 格式气象时序数据转换为固定地理范围的 PNG 图像序列的完整流程。生成的图像序列用于在 Cesium 三维地球引擎中实现逐小时轮播动画，并支持多气象变量图层的半透明叠加展示。

### 1.2 适用范围
- 输入数据：包含时间、经度、纬度维度的 NetCDF 文件（典型气象/海洋模型输出）。
- 输出产物：连续编号的 PNG 图像序列，每张图具有严格一致的地理范围、投影和颜色映射。
- 典型数据量：五天逐小时数据（精确为 **120 帧**），单帧尺寸约为 1800×1200 像素。

---

## 2. 输入数据规范

### 2.1 NetCDF 文件要求
| 项目     | 要求                                                         |
| :------- | :----------------------------------------------------------- |
| **维度** | 必须包含 `time`、`lat`（或 `latitude`）、`lon`（或 `longitude`）三个维度。 |
| **变量** | 至少一个数据变量（如 `temperature`、`precipitation`），其维度顺序为 `(time, lat, lon)` 或 `(time, lon, lat)`。 |
| **坐标** | 经纬度坐标值必须为单调递增或递减的一维数组，单位为度（WGS84）。 |
| **时间** | 时间坐标可为 `datetime64[ns]` 类型或带 `units` 属性的数值偏移量。<br>**本次目标时间范围（注意UTC 与 北京时）**：2026-03-20 00:00:00 至 2026-03-24 23:00:00。 |

### 2.2 预处理要求
- 数据中不应存在缺失值（NaN）或已通过插值/填充处理。
- 若原始数据经纬度范围大于目标区域，脚本将自动裁剪，无需预裁剪。

---

## 3. 输出产物规范

### 3.1 输出目录结构

每个变量子目录内包含 120 张以时间戳命名的 PNG 文件，命名格式为 `YYYYMMDDHHmm.png`（精确到分钟，因数据为整点时刻，分钟部分固定为 `00`）。

```
<ROOT_OUTPUT_DIR>/
├── temperature/
│   ├── 202603200000.png
│   ├── 202603200100.png
│   ├── ...
│   └── 202603242300.png
├── soil_moisture/
│   ├── 202603200000.png
│   ├── 202603200100.png
│   ├── ...
│   └── 202603242300.png
├── radiation_heat/
│   ├── 202603200000.png
│   ├── 202603200100.png
│   ├── ...
│   └── 202603242300.png
└── evaporation_runoff/
    ├── 202603200000.png
    ├── 202603200100.png
    ├── ...
    └── 202603242300.png
```

> **命名规则详解**：
>
> - 格式：`%Y%m%d%H%M.png`（Python `strftime` 格式）。
> - 示例：`202603200000.png` 表示 **2026年3月20日 00:00**，`202603242300.png` 表示 **2026年3月24日 23:00**。
> - 分钟部分固定为 `00`，与逐小时数据一致。
> - 每个子目录内严格包含 **120 张**时间连续的 PNG 文件。

### 3.2 图像规格

| 属性         | 规定值                                         | 说明                                                         |
| :----------- | :--------------------------------------------- | :----------------------------------------------------------- |
| **格式**     | PNG (Portable Network Graphics)                | 支持透明通道                                                 |
| **背景**     | 透明                                           | 便于多图层叠加                                               |
| **尺寸**     | 可配置，默认 1800×1200 像素 (12"×8" @ 150 DPI) | 保证清晰度且文件体积可控                                     |
| **地理范围** | **北纬 33.0° ~ 53.0°，东经 73.0° ~ 135.0°**    | 所有帧、所有变量均严格固定此范围                             |
| **投影**     | PlateCarree (EPSG:4326)                        | 等经纬度投影，直接对应 Cesium 地理坐标                       |
| **颜色映射** | 每个变量独立计算全局 `vmin` / `vmax`           | 同一变量的所有帧使用相同色阶，确保可比性；不同变量可使用不同色阶。需要支持配置色阶。 |
| **文件名**   | `YYYYMMDDHHmm.png`（例如 `202603200000.png`）  | 时间戳直接编码于文件名，便于前端按时间请求                   |

### 3.3 图像内容要求

- 必须包含海岸线和国界线参考要素（线宽 0.5，浅色）。
- 必须包含色标（Colorbar）及变量名称标签。
- 必须包含当前帧的时间戳标题（格式：`YYYY-MM-DD HH:MM`，例如 `2026-03-20 00:00`）。
- **对于不同变量**：推荐使用符合领域习惯的颜色映射（

---

## 4. 技术实现流程

### 4.1 工具链
- **Python 3.12**
- **xarray**：NetCDF 读取与处理
- **matplotlib**：图像渲染
- **cartopy**：地理投影与地图要素

### 4.2 核心步骤

```
┌─────────────────┐
│ 1. 读取 NetCDF  │
└────────┬────────┘
         ▼
┌─────────────────┐
│ 2. 裁剪至固定区域│
└────────┬────────┘
         ▼
┌─────────────────┐
│ 3. 加载领域标准色标与固定范围 │
└────────┬────────┘
         ▼
┌─────────────────┐
│ 4. 遍历时间维    │
│    ├─ 创建画布   │
│    ├─ 绘制数据   │
│    ├─ 添加地理要素│
│    ├─ 固定范围   │
│    └─ 保存 PNG   │
└─────────────────┘
```

### 4.3 关键约束
- **裁剪边界**：必须使用 `xarray.DataArray.sel()` 的 `slice` 方法，确保包含边界。
- **颜色范围**：领域标准色标与固定范围。
- **投影一致性**：`imshow` 的 `extent` 参数与 `set_extent` 参数必须使用相同的经纬度边界。
- **透明背景**：`plt.savefig(transparent=True)` 并配合 `bbox_inches='tight'` 去除白边。

---

## 5. 配置参数说明

| 参数名        | 类型        | 示例值              | 说明                    |
| :------------ | :---------- | :------------------ | :---------------------- |
| `NC_FILE`     | str         | `"forecast.nc"`     | 输入文件路径            |
| `VAR_NAME`    | str         | `"t2m"`             | 要提取的变量名          |
| `OUTPUT_DIR`  | str         | `"./frames_temp"`   | 输出目录                |
| `LON_RANGE`   | list[float] | **`[73.0, 135.0]`** | 经度范围 [min, max]     |
| `LAT_RANGE`   | list[float] | **`[33.0, 53.0]`**  | 纬度范围 [min, max]     |
| `FIG_WIDTH`   | float       | `12`                | 图片宽度（英寸）        |
| `FIG_HEIGHT`  | float       | `8`                 | 图片高度（英寸）        |
| `DPI`         | int         | `150`               | 分辨率（点/英寸）       |
| `CMAP`        | str         | `"viridis"`         | Matplotlib 颜色映射名称 |
| `TRANSPARENT` | bool        | `True`              | 是否透明背景            |
