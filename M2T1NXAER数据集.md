[NASA 戈达德地球科学数据与信息服务中⼼](https://disc.gsfc.nasa.gov/datasets/M2T1NXAER_5.12.4/summary)（M2T1NXAER数据集）:支持从1980至今的主要的气溶胶种类和气体前体物的数据下载。

根据官方说明，计算PM10需要您**自行组合MERRA-2提供的各物种组分数据**。这是一个标准且直接的操作，请按以下步骤进行：

### 第一步：下载必需的原始数据变量

您需要从 **`M2T1NXAER_5.12.4`**（逐小时数据）或对应的月平均数据集中，下载以下**地表质量浓度**变量（单位：kg/m³）。这些是计算PM2.5和PM10的“零件”。

| 变量名          | 中文描述                              | 在PM计算中的角色   |
| --------------- | ------------------------------------- | ------------------ |
| **`BCSMASS`**   | 黑碳地表质量浓度                      | PM2.5的组成部分    |
| **`OCSMASS`**   | 有机碳地表质量浓度                    | PM2.5的组成部分    |
| **`SO4SMASS`**  | 硫酸盐地表质量浓度                    | PM2.5的组成部分    |
| **`DUSMASS25`** | 沙尘（细模态，PM2.5部分）地表质量浓度 | PM2.5的组成部分    |
| **`SSSMASS25`** | 海盐（细模态，PM2.5部分）地表质量浓度 | PM2.5的组成部分    |
| **`DUSMASS`**   | 沙尘（总，含粗、细模态）地表质量浓度  | 用于计算粗模态沙尘 |
| **`SSSMASS`**   | 海盐（总，含粗、细模态）地表质量浓度  | 用于计算粗模态海盐 |

**操作提示**：在GES DISC数据子集工具中，请确保勾选以上7个变量，并选择您研究所需的时间和地理范围。

### 第二步：应用标准计算公式

下载数据后，使用以下公式进行计算。假设所有变量已读取为相同维度的数组（时间，纬度，经度）。

1. **计算 PM2.5**（单位：kg/m³） `PM2.5 = BCSMASS + OCSMASS + SO4SMASS + DUSMASS25 + SSSMASS25`
2. **计算粗模态质量浓度** 粗模态沙尘 = `DUSMASS - DUSMASS25` ;粗模态海盐 = `SSSMASS - SSSMASS25`
3. **计算 PM10**（单位：kg/m³） `PM10 = PM2.5 + (DUSMASS - DUSMASS25) + (SSSMASS - SSSMASS25)` **简化公式**：`PM10 = BCSMASS + OCSMASS + SO4SMASS + DUSMASS + SSSMASS`

### 第三步：单位转换（通常需要）

MERRA-2质量浓度的原始单位是 **kg/m³**，而环境科学中常用 **μg/m³**。转换公式为：

```
浓度 (μg/m³) = 浓度 (kg/m³) × 1,000,000,000
```



所有的变量名都遵循一个通用结构：`[组分代号][后缀]`

- **`[组分代号]`**：如 `BC`（黑碳）、`DU`（沙尘）、`OC`（有机碳）、`SS`（海盐）、`SO4`（硫酸盐）、`TOT`（总和）。
- **`[后缀]`**：表示该变量的**物理量和描述方式**。其含义和用途如下表所示：

| 后缀                     | 全称与含义                                                   | 主要用途                                                     | 在PM计算中的角色                           |
| ------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------ |
| **`SMASS`**              | **Surface Mass Concentration**  **地表质量浓度**             | 研究近地面污染、健康影响、计算**PM2.5/PM10**。               | **核心**。计算PM时必须使用此系列。         |
| **`CMASS`**              | **Column Mass Density**  **垂直气柱质量密度**                | 研究大气中该成分的总含量（负荷）。                           | 不用于计算地表PM，但可用于分析气溶胶总量。 |
| **`EXTTAU`**             | **Extinction Aerosol Optical Thickness**  **消光气溶胶光学厚度** (550nm) | 评估气溶胶对太阳辐射的总体削弱（散射+吸收）效应，是研究**气候辐射强迫**的关键参数。 | 不直接用于PM计算，属于光学特性。           |
| **`SCATAU`**             | **Scattering Aerosol Optical Thickness**  **散射气溶胶光学厚度** (550nm) | 评估气溶胶对太阳辐射的散射效应。                             | 不直接用于PM计算，属于光学特性。           |
| **`ANGSTR`**             | **Angstrom Exponent**  **安格斯特朗指数** (470-870nm)        | 推断气溶胶粒径大小。**值越大，粒子越细**（如人为污染）；**值越小，粒子越粗**（如沙尘、海盐）。 | 不用于计算，但可用于判断污染类型。         |
| **`FLUXU`**  **`FLUXV`** | **Column Mass Flux (U/V方向)**  **垂直积分质量通量**         | 分析气溶胶的**水平输送**方向和强度（U为东西，V为南北），用于**溯源和追踪传输路径**。 | 不用于浓度计算，用于过程分析。             |



### 总结与选择建议

| 变量           | 物理意义                                             | 单位      | 在PM计算中的角色                                             |
| -------------- | ---------------------------------------------------- | --------- | ------------------------------------------------------------ |
| **`SO4CMASS`** | 硫酸盐**柱质量密度**so4 column mass density          | **kg/m²** | **错误**。单位不匹配，且代表整层大气总量，**不能**用于计算地表PM浓度。 |
| **`SO4SMASS`** | 硫酸盐**地表质量浓度**so4 surface mass concentration | **kg/m³** | **正确**。这是计算PM2.5/PM10时所需的硫酸盐浓度，必须乘以1.375后使用。 |





中国标准空气质量指数 (AQI) 完整颜色映射表

| **AQI 数值**  | **级别** | **颜色** | **RGB 数值**    | **十六进制 (Hex)** |
| ------------- | -------- | -------- | --------------- | ------------------ |
| **0 - 50**    | 优       | 🟢 绿色   | `(0, 228, 0)`   | **`#00E400`**      |
| **51 - 100**  | 良       | 🟡 黄色   | `(255, 255, 0)` | **`#FFFF00`**      |
| **101 - 150** | 轻度污染 | 🟠 橙色   | `(255, 126, 0)` | **`#FF7E00`**      |
| **151 - 200** | 中度污染 | 🔴 红色   | `(255, 0, 0)`   | **`#FF0000`**      |
| **201 - 300** | 重度污染 | 🟣 紫色   | `(153, 0, 76)`  | **`#99004C`**      |
| **> 300**     | 严重污染 | 🟤 褐红色 | `(126, 0, 35)`  | **`#7E0023`**      |
